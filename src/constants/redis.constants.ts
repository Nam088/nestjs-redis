/**
 * Token for Redis module options provider
 */
export const REDIS_MODULE_OPTIONS = 'REDIS_MODULE_OPTIONS';

/**
 * Prefix for Redis client provider tokens
 */
export const REDIS_CLIENT = 'REDIS_CLIENT';

/**
 * Default name for Redis connection
 */
export const DEFAULT_REDIS_NAME = 'default';
